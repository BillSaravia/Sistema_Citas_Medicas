import { environment } from "src/environments/environment.development";
import { UikitRoutingModule } from '../core/components/uikit/uikit-routing.module';

export const URL_BACKEND = environment.URL_BACKEND;
export const ULR_SERVICIOS = environment.ULR_SERVICIOS;
export const URL_FRONTEND  = environment.URL_FRONTEND;